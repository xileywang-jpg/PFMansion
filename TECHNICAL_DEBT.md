# 技术债记录

> 记录项目中已知的技书债和问题，供后续开发参考
> 创建时间: 2026-03-27

---

## 概述

本文档记录项目中的技术债、架构决策和待改进项。目的是：
1. 避免未来开发中引入不兼容变更
2. 为后续重构提供参考
3. 追踪已知问题的上下文

---

## P2 级技术债

### TD-01: 中文字符串解析属性 ⚠️

**位置**: `store/gameStore.ts` - `parseAttributeFromText` 函数

**问题**: 
技能/物品的被动效果使用中文字符串匹配属性类型：
```typescript
if (text.includes('力量')) attr = AttributeName.Might;
```

**风险**:
- 文案微调可能导致解析失败
- 维护困难，新加入的文案需要遵循固定格式

**改进方向**:
- 将被动效果改为结构化数据格式:
  ```typescript
  passiveEffects: [{ type: "buff", attribute: "might", modifier: 1 }]
  ```
- 这需要同步修改所有现有数据文件

**状态**: 暂不处理，等待大规模数据重构

---

### TD-02: 前端网络层冗余代码 ⚠️

**位置**: 
- `ws/adapter.ts` - 死代码
- `hooks/useGameNetwork.ts` - 部分废弃

**问题**:
- `ws/adapter.ts` 的 `syncFromNetwork` 从未被调用
- `useGameNetwork.ts` 的 `syncGameState` 是空实现
- 两个文件各自维护了 `isNetworkMode` 检查函数

**改进方向**:
- 删除 `ws/adapter.ts`
- 标记 `useGameNetwork.ts` 为废弃，仅保留房间管理接口
- 统一使用 `ws/network.ts` 的 `isInNetworkMode()`

**状态**: 建议后续清理

---

### TD-03: 战斗流程无 DEFENDING 阶段

**位置**: 
- 前端: `store/gameStore.ts` (已清理)
- 后端: `game/combat.go`

**问题**:
- 后端战斗流程简化为: 开始 → 结算
- 没有独立的 DEFENDING 阶段
- 前端 UI 不应假设存在此阶段

**决策**: 
- 保持后端简化设计
- 前端已移除 DEFENDING 定义
- 后续如需更复杂战斗流程（如防御动作选择），需扩展后端

**状态**: 已知，不影响当前 MVP

---

### TD-04: 个人日志仅存前端 ⚠️

**位置**: 
- 前端: `store/gameStore.ts` (personalLogs)
- 后端: `GamePlayer` 结构

**问题**:
- `personalLogs` 只存在于前端
- 页面刷新或重连后会丢失
- 每个玩家的个人日志不广播给其他人（设计如此）

**改进方向**:
- 在后端 `GamePlayer` 添加 `PersonalLogs` 字段
- 重连时通过单播同步给对应玩家
- 或实现日志重放机制

**状态**: 暂不处理，当前 MVP 不需要重连后保留日志

---

## P3 级技术债

### TD-05: NextTurn/nextTurnInternal 重复逻辑

**位置**: `game/logic.go`

**问题**:
- `NextTurn` (公开) 和 `nextTurnInternal` (内部) 核心逻辑重复
- 维护困难，可能导致不一致

**改进方向**:
- 统一为一个内部实现
- 公开接口仅负责加锁后调用

**状态**: 建议后续重构

---

### TD-06: 日志排序方向不一致

**位置**: 
- 前端 `addLog`: prepend (新日志在前)
- 后端 `state_sync`: append (新日志在后)

**问题**:
- 每次同步后日志顺序可能跳动

**改进方向**:
- 统一排序方向
- 或在 `handleStateSync` 中反转后端日志

**状态**: 低优先级

---

## 已修复问题记录

以下问题已在开发过程中修复，记录以供追溯：

| 日期 | 问题 | 修复内容 |
|------|------|---------|
| 2026-03-27 | BUG-01 | ResolveCombat 双重赋值导致骰子修改失效 |
| 2026-03-27 | BUG-04 | PETRIFIED continue 跳转错误 |
| 2026-03-27 | BUG-02 | Attribute 是值类型，战斗结算需显式写回 |
| 2026-03-27 | MISS-05 | initializeGame 在网络模式仍执行 |
| 2026-03-27 | FLOW-01 | 日志合并导致无限增长 |
| 2026-03-27 | FLOW-04 | 前端 DEFENDING 阶段定义与后端不一致 |

---

## 架构决策记录

### ADC-01: 战斗系统简化

**日期**: 2026-03-27

**决策**: 
- 战斗简化为单次结算，无多轮攻防
- 骰子修改通过钩子机制支持
- 未来可扩展为多轮战斗

### ADC-02: 物品获取方式

**日期**: 2026-03-27

**决策**:
- ITEM: 默认自动获得
- OMEN: 揭示时自动获得（作祟检定后由成功者获得）
- 未来可添加"掉落"机制（物品放到地面）

### ADC-03: NPC 角色

**日期**: 2026-03-27

**决策**:
- NPC 仅作为被动攻击目标
- 不实现自动 AI 移动/追踪
- 未来可扩展 NPC 主动行为

---

## 注意事项

1. **属性效果必须使用结构化数据**: 当前允许使用中文字符串，但新加的被动效果应避免
2. **网络模式下前端不应直接修改状态**: 所有游戏逻辑由后端驱动
3. **避免在 `nextTurnInternal` 外调用持锁函数**: 统一锁管理
