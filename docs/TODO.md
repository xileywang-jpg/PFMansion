# 统一待办清单

本文件只保留未完成项。已完成的事项不再继续留在 TODO 中。

## 1. MVP 可后置但明确存在

### 1.1 Objective 复合规则模型

- 支持 AND/OR、多条件组合、多个计数器或多阶段 objective

必要性：中。

原因：属于扩展能力，不是当前干净闭环的最低前提。

### 1.2 Passive 高级触发器

- 支持多条件组合、更多上下文过滤、非战斗复合触发条件

必要性：中。

原因：当前结构化 passive 已能覆盖 MVP 主链，但高级表达还未完成。

### 1.3 Narrative / System Strings 模板化

- 将更多系统文案从代码中抽为模板或配置

必要性：低。

原因：提升可维护性，但不是当前最阻塞的闭环缺口。

## 2. 长期选项

### 2.1 共享 schema / 代码生成

- 如果 Go / TS schema 后续继续频繁漂移，再评估共享 schema、代码生成或单独 schema 包

必要性：低。

原因：当前通过文档、loader 校验和运行时边界脚本还能维持，但长期可能值得做。