# 🕯️ Mansion Protocol 核心功能与架构逻辑

## 1. 游戏机制概览
《Mansion Protocol》是一款数据驱动的氛围探索桌游，游戏分为两个主要阶段：

### 1.1 探索阶段 (Exploration)
*   **动态地图生成**：玩家通过移动探索未知区域。系统从 `TILE_DECK` 随机抽取房间，并要求玩家旋转房间以匹配门洞位置。
*   **事件与预兆**：进入新房间会触发 `EVENT`（属性检定或选择）、`ITEM`（直接获得道具）或 `OMEN`（立即进行作祟检定，通过后自动获得预兆）。

### 1.2 作祟阶段 (Haunt)
*   **作祟检定**：每次揭示预兆，玩家立即进行 6 骰子检定。若结果小于当前总预兆数，作祟爆发。
*   **剧本矩阵**：根据“最后一张预兆”+“当前所在房间”的组合，从 `hauntMatrix.ts` 确定唯一的剧本 ID。
*   **叛徒判定**：根据剧本规则（如：力量最高者、理智最低者）自动分配叛徒。

## 2. 核心逻辑引擎 (Logic DSL)
为了实现高度的可扩展性，项目引入了 **JSON-Driven Logic** 系统 (`types/Logic.ts`)：

*   **Condition (条件)**：支持嵌套 `AND/OR`、属性比较 (`GT/LT/EQ`) 和道具持有检查。
*   **Effect (效果)**：定义了 `MODIFY_STAT`、`MOVE`、`DRAW_CARD` 等原子操作。
*   **Target Selector (目标选择器)**：逻辑引擎可以动态寻找受众，例如 `NEAREST_ENEMY`（最近的敌人）会自动计算曼哈顿距离。
*   **Interpreter (解释器)**：当前正式结算由 Go 后端负责；前端仅保留 `types/Logic.ts` 作为数据结构定义，不再执行本地逻辑解释器。

## 3. 热座模式设计 (Hot-seat Friendly)
由于多名玩家在同一台设备上游戏，隐私保护至关重要：

*   **揭晓遮罩 (`HauntReveal`)**：剧本开始时，采用多步确认制。只有当对应的玩家确认“设备已移交”时，才会显示其私密的剧本目标（英雄目标 vs 叛徒目标）。
*   **折叠式 HUD**：HUD 中的剧本目标默认折叠，防止非当前回合玩家在观察屏幕时误看他人的秘密能力。

## 4. 视觉与氛围
*   **配色方案**：采用 Zinc-950 (深色) 与 Indigo (英雄)/Red (叛徒) 的高对比度设计。
*   **动态反馈**：使用 `Framer Motion` 实现流畅的地块平移、骰子翻转动画。
*   **反馈系统**：通过 `FeedbackToast` 在屏幕中央显示重大的回合切换与角色死亡信息。
